[toolchain]
# Fork pinned to nightly to satisfy dorsid's #![feature(...)] requirements
# (see plans/okay-let-s-do-the-replicated-shell.md, D1).
channel = "nightly"
components = [ "rustfmt", "clippy" ]
targets = ["wasm32-unknown-unknown"]
