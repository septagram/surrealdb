/**
[env]
namespace = true
database = true
imports = ["gql/_schema.surql"]

[test]
reason = "SKIP and LIMIT paging after ordering."

[[test.results]]
value = "[{ name: 'B' }]"
*/

MATCH (n:person) RETURN n.name AS name ORDER BY name SKIP 1 LIMIT 1
