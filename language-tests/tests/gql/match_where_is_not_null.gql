/**
[env]
namespace = true
database = true
imports = ["gql/_schema.surql"]

[test]
reason = "IS NOT NULL excludes rows whose property is missing."

[[test.results]]
value = "[{ name: 'A' }, { name: 'B' }]"
*/

MATCH (n:person) WHERE n.age IS NOT NULL RETURN n.name AS name ORDER BY name
