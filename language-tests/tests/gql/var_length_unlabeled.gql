/**
[env]
namespace = true
database = true
imports = ["gql/_schema.surql"]

[test]
reason = "Variable-length {1,2} with an unlabeled far node under V2_DESIGN R6 (one row per path at depths 1..2, edge-unique per path). The unlabeled far node lets city:1 surface as a target. v1 returned eleven rows (distinct reachable nodes); v2 returns twelve because target A is reached by two distinct two-hop paths into person:1. Hand-derivation (edges 1->2,2->1,2->3,3->1,1->city): from A (1): 2,city / 1,3; from B (2): 1,3 / 2,city,1; from C (3): 1 / 2,city. Names -> A:4 (one from A's d2, two from B, one from C), B:3, C:2, London:3 = 12 rows."

[[test.results]]
value = "[{ name: 'A' }, { name: 'A' }, { name: 'A' }, { name: 'A' }, { name: 'B' }, { name: 'B' }, { name: 'B' }, { name: 'C' }, { name: 'C' }, { name: 'London' }, { name: 'London' }, { name: 'London' }]"
*/

MATCH (a:person)-[:knows]->{1,2}(b) RETURN b.name AS name ORDER BY name
