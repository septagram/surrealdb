/**
[env]
namespace = true
database = true
imports = ["gql/_schema.surql"]

[test]
reason = "Smoke test for the .gql dialect plumbing: the file is parsed by the GQL parser, lowered onto the SurrealQL AST, and executed through the normal Datastore::process path."

[[test.results]]
value = "[{ name: 'A' }, { name: 'B' }, { name: 'C' }]"
*/

MATCH (n:person) RETURN n.name AS name ORDER BY name
