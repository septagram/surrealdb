/**
[test]
reason = "Lowering rejection: a path-search prefix requires a variable-length segment; a fixed single hop has nothing to search."

[test.results]
parsing-error = """
A path search prefix requires a single variable-length segment, e.g. `(a)-[:edge]->{1,3}(b)`
  --> [15:7]
   |
15 | MATCH ANY SHORTEST (a:wp)-[:road]->(b:wp) RETURN a.name AS an
   |       ^^^^^^^^^^^^ quantify exactly one edge (`{m,n}`, `+`, `*`)
"""
*/

MATCH ANY SHORTEST (a:wp)-[:road]->(b:wp) RETURN a.name AS an
